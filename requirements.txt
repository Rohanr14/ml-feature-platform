# Core numeric stack — pin numpy<2 for binary compatibility with pandas/deltalake/feast
numpy<2

# Data processing
pandas
pyarrow
duckdb

# Kafka
confluent-kafka

# Delta Lake
deltalake

# Feature store
feast[local]

# ML
scikit-learn
torch
mlflow>=2.11,<3
boto3          # required for MLflow artifact upload to MinIO (S3-compatible)

# Serving
fastapi
uvicorn[standard]
prometheus-client

# Config / validation
pydantic
pydantic-settings
pyyaml

# RAG
langchain-core
psycopg[binary]
