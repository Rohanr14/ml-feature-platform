name: "ml_feature_platform"
version: "0.1.0"
config-version: 2

profile: "ml_feature_platform"
model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ml_feature_platform:
    staging:
      +materialized: view
    features:
      +materialized: table
